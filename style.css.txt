* { margin:0; padding:0; box-sizing:border-box; font-family:'Poppins',sans-serif; }
html, body { overflow-x:hidden; background:#0f0524; color:#fff; min-height:100vh; }

.bg { position:fixed; inset:0; z-index:0; overflow:hidden;
  background: radial-gradient(ellipse at top, #1c0b3a 0%, #0f0524 70%); }
.blob { position:absolute; border-radius:50%; filter:blur(80px); opacity:0.55; }
.b1 { width:500px; height:500px; background:#ff4d6d; top:-100px; left:-100px; animation:float1 18s infinite; }
.b2 { width:400px; height:400px; background:#7b2ff7; bottom:-100px; right:-100px; animation:float2 22s infinite; }
.b3 { width:350px; height:350px; background:#ff8fa3; top:40%; left:50%; animation:float3 25s infinite; }
@keyframes float1 { 0%,100%{transform:translate(0,0);} 50%{transform:translate(150px,100px);} }
@keyframes float2 { 0%,100%{transform:translate(0,0);} 50%{transform:translate(-120px,-100px);} }
@keyframes float3 { 0%,100%{transform:translate(-50%,-50%);} 50%{transform:translate(-30%,-60%);} }

.floating-emoji { position:fixed; inset:0; pointer-events:none; z-index:1; overflow:hidden; }
.float-em { position:absolute; animation:floatUp linear forwards; font-size:24px; opacity:0.9; }
@keyframes floatUp {
  0% { transform: translateY(0) scale(0.6); opacity:0; }
  10% { opacity:1; }
  100% { transform: translateY(-110vh) scale(1.1) rotate(360deg); opacity:0; }
}

.music-btn { position:fixed; top:18px; right:18px; z-index:100;
  width:46px; height:46px; border-radius:50%;
  border:1px solid rgba(255,255,255,0.25);
  background: rgba(255,255,255,0.08); backdrop-filter: blur(10px);
  color:#fff; font-size:20px; cursor:pointer; transition:0.3s; }
.music-btn:hover { transform:scale(1.1); background:rgba(255,255,255,0.18); }
.music-btn.on { animation:pulse 1.5s infinite; }
@keyframes pulse {
  0%,100% { box-shadow: 0 0 0 0 rgba(255,77,109,0.6); }
  50%     { box-shadow: 0 0 0 12px rgba(255,77,109,0); }
}

.scoreboard { position:fixed; top:18px; left:18px; z-index:100;
  padding:10px 18px; border-radius:30px;
  background: rgba(255,255,255,0.08); backdrop-filter: blur(10px);
  border:1px solid rgba(255,255,255,0.15); font-size:0.9rem; }
.scoreboard.hidden { display:none; }
.scoreboard b { color:#ffb3c6; }

.screen { position:fixed; inset:0; display:none; flex-direction:column;
  justify-content:center; align-items:center; padding:30px 20px;
  z-index:10; opacity:0; transform: translateY(30px);
  transition: opacity 0.6s, transform 0.6s; overflow-y:auto; }
.screen.active { display:flex; opacity:1; transform: translateY(0); }

.intro-box { text-align:center; max-width:520px; }
.gift { font-size:5rem; animation:bounce 1.5s infinite; margin-bottom:20px; }
@keyframes bounce {
  0%,100% { transform: translateY(0) rotate(-5deg); }
  50%     { transform: translateY(-20px) rotate(5deg); }
}
.intro-title { font-family:'Fredoka',sans-serif; font-size:2.5rem;
  background:linear-gradient(135deg,#ff8fa3,#ffd6e0,#c41e5c);
  -webkit-background-clip:text; -webkit-text-fill-color:transparent;
  background-clip:text; margin-bottom:15px; }
.intro-text { font-size:1.15rem; line-height:1.6; color:#e0d0ff; margin-bottom:15px; }
.intro-text b { color:#ffb3c6; }
.intro-sub { font-size:1rem; color:#b8a8d6; margin-bottom:25px; }

.btn-primary { padding:15px 40px; font-family:'Poppins',sans-serif;
  font-size:1.05rem; font-weight:600; letter-spacing:0.5px;
  border:none; border-radius:50px; cursor:pointer; color:#fff;
  background:linear-gradient(135deg,#ff4d6d,#c41e5c);
  box-shadow: 0 12px 30px rgba(255,77,109,0.5);
  transition:0.3s; }
.btn-primary:hover { transform: translateY(-3px) scale(1.05);
  box-shadow: 0 18px 40px rgba(255,77,109,0.7); }
.btn-primary:disabled { opacity:0.5; cursor:not-allowed; }

.game-wrap { width:100%; max-width:640px; text-align:center; }
.game-title { font-family:'Fredoka',sans-serif; font-size:1.8rem;
  background:linear-gradient(135deg,#ff8fa3,#ffd6e0);
  -webkit-background-clip:text; -webkit-text-fill-color:transparent;
  background-clip:text; margin-bottom:10px; }
.game-sub { color:#c8b8e6; margin-bottom:20px; font-size:0.95rem; }
.game-stats { display:flex; justify-content:center; gap:25px;
  margin-bottom:15px; font-size:1.1rem; }
.game-stats b { color:#ffb3c6; font-size:1.3rem; }

.play-area { position:relative; width:100%; height:380px;
  background: rgba(255,255,255,0.03);
  border:1px solid rgba(255,255,255,0.1);
  border-radius:24px; overflow:hidden; }
.start-overlay { position:absolute; inset:0; display:flex;
  align-items:center; justify-content:center;
  background:rgba(0,0,0,0.4); backdrop-filter: blur(4px); }
.start-overlay.hidden { display:none; }
.heart { position:absolute; font-size:32px; cursor:pointer;
  user-select:none; transition: transform 0.15s;
  animation: appear 0.3s ease-out; }
.heart:hover { transform: scale(1.3); }
.heart.popped { animation: pop 0.4s forwards; pointer-events:none; }
@keyframes appear { from { transform:scale(0); } to { transform:scale(1); } }
@keyframes pop { to { transform:scale(2); opacity:0; } }

.boxes { display:flex; justify-content:center; gap:20px;
  margin:30px 0; flex-wrap:wrap; }
.box { width:110px; height:110px; font-size:3rem;
  background:linear-gradient(135deg,#2a1550,#1a0b3a);
  border:2px solid rgba(255,255,255,0.15);
  border-radius:20px; display:flex; align-items:center;
  justify-content:center; cursor:pointer;
  transition: 0.3s; box-shadow: 0 10px 30px rgba(0,0,0,0.4); }
.box:hover { transform: translateY(-8px) scale(1.05);
  border-color: rgba(255,110,180,0.7); }
.box.opened { background:linear-gradient(135deg,#ff4d6d,#c41e5c);
  animation: popIn 0.5s; }
@keyframes popIn { 0% { transform:scale(0.5); } 100% { transform:scale(1); } }

.memory-grid { display:grid; grid-template-columns:repeat(4,1fr);
  gap:10px; max-width:440px; margin:20px auto; }
.mem-card { aspect-ratio:1; border-radius:14px;
  background:linear-gradient(135deg,#2a1550,#1a0b3a);
  border:1px solid rgba(255,255,255,0.15);
  display:flex; align-items:center; justify-content:center;
  font-size:2rem; cursor:pointer; transition: 0.3s;
  user-select:none; }
.mem-card:hover { transform:scale(1.05); }
.mem-card.flipped { background:linear-gradient(135deg,#ff8fa3,#ff4d6d);
  transform: rotateY(180deg); }
.mem-card.matched { background:linear-gradient(135deg,#6ee7b7,#10b981);
  pointer-events:none; }

.result-box { margin-top:20px; padding:22px; border-radius:18px;
  background: rgba(255,255,255,0.06); backdrop-filter: blur(10px);
  border:1px solid rgba(255,255,255,0.15); font-size:1.1rem;
  line-height:1.5; animation: slideUp 0.6s ease-out; }
.result-box.hidden { display:none; }
.result-box b { color:#ffb3c6; font-size:1.2rem; }
@keyframes slideUp { from { opacity:0; transform: translateY(20px);} to { opacity:1; transform: translateY(0);} }
.result-box .btn-primary { margin-top:15px; }

.final-box { text-align:center; max-width:560px; position:relative; }
.final-emoji { font-size:5rem; margin-bottom:15px;
  animation: bounce 2s infinite; }
.final-title { font-family:'Fredoka',sans-serif; font-size:2.2rem;
  background:linear-gradient(135deg,#ff4d6d,#ffd6e0,#c41e5c);
  -webkit-background-clip:text; -webkit-text-fill-color:transparent;
  background-clip:text; margin-bottom:20px; }
.final-line { font-size:1.15rem; line-height:1.7; color:#e0d0ff;
  margin-bottom:35px; }
.final-line b { color:#ffb3c6; }
.final-btns { display:flex; justify-content:center; gap:20px;
  flex-wrap:wrap; min-height:80px; position:relative; }
.btn-yes { padding:18px 55px; font-size:1.2rem; font-weight:700;
  border:none; border-radius:50px; color:#fff; cursor:pointer;
  background:linear-gradient(135deg,#ff4d6d,#c41e5c);
  box-shadow: 0 15px 40px rgba(255,77,109,0.6);
  transition: 0.3s; }
.btn-yes:hover { transform: scale(1.08); }
.btn-no { padding:18px 40px; font-size:1.05rem; font-weight:500;
  border:1px solid rgba(255,255,255,0.2); border-radius:50px;
  color:#fff; background: rgba(255,255,255,0.06);
  cursor:pointer; transition: 0.4s; backdrop-filter: blur(10px); }
.final-hint { margin-top:25px; font-size:0.95rem; color:#b8a8d6;
  min-height:1.4em; }

.success-box { text-align:center; max-width:600px; }
.success-emoji { font-size:6rem; animation: bounce 1s infinite;
  margin-bottom:20px; }
.success-title { font-family:'Fredoka',sans-serif; font-size:2.5rem;
  background:linear-gradient(135deg,#ff4d6d,#ffd6e0,#c41e5c);
  -webkit-background-clip:text; -webkit-text-fill-color:transparent;
  background-clip:text; margin-bottom:20px;
  animation: glowText 2s infinite alternate; }
@keyframes glowText {
  from { filter: drop-shadow(0 0 15px rgba(255,77,109,0.5)); }
  to   { filter: drop-shadow(0 0 40px rgba(255,77,109,1)); }
}
.success-text { font-size:1.15rem; line-height:1.7; color:#e0d0ff;
  margin-bottom:25px; }
.success-hearts { font-size:2rem; letter-spacing:10px;
  margin:20px 0; animation: bounce 3s infinite; }
.success-quote { font-style:italic; color:#ffb3c6;
  font-size:1.1rem; margin:25px 0; }

.dots { position:fixed; bottom:20px; left:50%;
  transform:translateX(-50%); display:flex; gap:8px;
  padding:8px 16px; background: rgba(0,0,0,0.3);
  backdrop-filter: blur(10px); border-radius:30px;
  border:1px solid rgba(255,255,255,0.1); z-index:100; }
.dot { width:8px; height:8px; border-radius:50%;
  background: rgba(255,255,255,0.3); transition: 0.3s; }
.dot.active { background: #ff4d6d; box-shadow: 0 0 10px #ff4d6d;
  transform: scale(1.4); }
.dot.done { background: rgba(255,110,180,0.6); }

#fx { position:fixed; inset:0; pointer-events:none; z-index:500; }

@media (max-width:600px) {
  .intro-title { font-size:2rem; }
  .game-title { font-size:1.4rem; }
  .play-area { height:320px; }
  .box { width:85px; height:85px; font-size:2.2rem; }
  .memory-grid { grid-template-columns:repeat(4,1fr); max-width:340px; }
  .mem-card { font-size:1.5rem; }
  .final-title { font-size:1.7rem; }
  .success-title { font-size:1.9rem; }
  .btn-yes { padding:15px 40px; font-size:1.05rem; }
  .btn-no { padding:15px 28px; font-size:0.95rem; }
  .scoreboard { font-size:0.8rem; padding:8px 14px; }
}